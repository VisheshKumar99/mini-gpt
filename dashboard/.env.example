# Template for the dashboard environment config.
# Copy this file to `.env` and adjust the values for your setup:
#   cp .env.example .env

# Base URL of the FastAPI backend (HTTP requests)
VITE_API_BASE_URL=http://localhost:8000

# Base URL of the FastAPI backend (WebSocket connections)
VITE_WS_BASE_URL=ws://localhost:8000
